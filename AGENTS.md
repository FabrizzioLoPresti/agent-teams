# AGENTS.md

Authoritative quick-reference for AI agents working in this repo. Every entry answers: "would an agent likely miss this?"

## Communication Style

**Be concise.** Answer with the minimum necessary. No preamble, no summaries, no filler. Short answers only.

---

## Developer Commands

All commands run via `pnpm`. All `db:*` commands load from `.env.local` (via `dotenv-cli`) — **not** `.env`.

```bash
pnpm dev                  # dev server on :3000
pnpm build                # vite build + cp instrument.server.mjs .output/server
pnpm start                # production start (requires prior build)
pnpm test                 # vitest run (non-watch)
pnpm lint                 # eslint
pnpm check                # prettier --write . && eslint --fix

pnpm db:generate          # regenerate Prisma client from schema
pnpm db:migrate           # create + apply migration (prompts for name)
pnpm db:push              # schema push without a migration file
pnpm db:seed              # run tools/scripts/seed.ts
pnpm db:studio            # Prisma Studio GUI
```

Run a single test file: `pnpm vitest run src/path/to/file.test.ts`

---

## First-Time Setup Order

1. `pnpm install`
2. Copy `.env.example` → `.env.local` and fill in values
3. `docker compose up -d` — starts Postgres 16 + Redis
4. `pnpm db:migrate` — applies migrations and regenerates Prisma client
5. `pnpm dev`

---

## Generated Files — Never Edit Manually

| File / Dir | Regenerated by |
|---|---|
| `generated/prisma/` | `pnpm db:generate` (also runs at end of `db:migrate`) |
| `src/routeTree.gen.ts` | Auto-updated by `@tanstack/router-plugin` on `dev`/`build` |

`generated/prisma/` is in `.gitignore` — regenerate after cloning. `src/routeTree.gen.ts` is checked in.

---

## Environment Variables

- **Server**: validated in `src/env/server.ts` (uses `@t3-oss/env-core`). Required: `DATABASE_URL`, `REDIS_URL`, `BETTER_AUTH_URL`, `BETTER_AUTH_SECRET`, `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`.
- **Client**: validated in `src/env/client.ts`. Must have `VITE_` prefix. Required: `VITE_BETTER_AUTH_URL`.
- **Never** access `process.env` directly — always import from `@/env/server` or `@/env/client`.
- `src/env.ts` (root) is a legacy stub — it is NOT authoritative.

---

## Code Conventions That Differ From Defaults

### No manual memoization
**React Compiler is active** (`babel-plugin-react-compiler`). Do NOT add `useMemo`, `useCallback`, or `React.memo`. The compiler handles all memoization automatically.

### No direct oRPC calls in components
All server calls go through `src/data/<domain>/<verb>-<entity>.ts` hooks. Never call `client` or `orpc` from `src/orpc/client.ts` directly in a component.

### Path alias
`@/` → `src/`. Always use alias imports; never use deep relative paths.

### Code style (Prettier)
- No semicolons (`semi: false`)
- Single quotes
- Trailing commas everywhere

### Shadcn components
```bash
pnpx shadcn@latest add <component>   # note: pnpx, not npx
```
Components land in `src/components/ui/`. Config in `components.json` (style: `new-york`, base color: `zinc`).

---

## Layer Map

| Layer | Location |
|---|---|
| Zod schemas | `src/orpc/schemas/<domain>.ts` |
| oRPC handlers | `src/orpc/router/<domain>.ts` |
| oRPC router index | `src/orpc/router/index.ts` |
| React Query hooks | `src/data/<domain>/<verb>-<entity>.ts` |
| TypeScript types | `src/types/<domain>.ts` (all are `z.infer<typeof ...Schema>`) |
| Config / constants | `src/config/<domain>.ts` |
| Routes | `src/routes/<prefix>/<section>/` |
| Shadcn UI components | `src/components/ui/` |
| Domain components | `src/components/<domain>/` |
| Utilities | `src/utils/<domain>.ts` |
| DB client (Prisma) | `src/db/db.ts` → import as `import { prisma } from '@/db/db'` |
| Redis client | `src/db/redis.ts` |
| Auth server | `src/lib/auth/auth.ts` |
| Auth client | `src/lib/auth/auth-client.ts` |
| Zustand stores | `src/store/<name>.ts` |

---

## Route Prefixes

| Prefix | Audience |
|---|---|
| `_general/` | Public |
| `_customers/` | `customerComplex` role |
| `_owners/` | `ownerComplex` role |
| `_users/` | Any authenticated user |
| `auth/` | Auth pages |
| `api/` | API handlers (oRPC + Better Auth) |

All API traffic goes through `/api/rpc`. No REST endpoints.

---

## Auth

- Library: Better Auth v1.4
- Three fixed roles set at registration: `user`, `customerComplex`, `ownerComplex`. **Role cannot change after creation.**
- Role-based redirects: `customerComplex` → `/search`, `ownerComplex` → `/dashboard`, `user` → `/`
- oRPC middleware:
  - Public procedures: `baseInputValidationMiddleware`
  - Protected procedures: `authorizedMiddleware` (adds `context.session` and `context.user`, throws `UNAUTHORIZED` if no session)
- Route-level guard: `src/middlewares/auth.ts` — redirects to `/auth/login`
- Session storage: Redis. Session TTL: 7 days.
- Social login: Google only (Apple fields in `.env.example` are not implemented).

---

## Database

- PostgreSQL 16 via Docker; Prisma v7 with `@prisma/adapter-pg` (explicit driver adapter pattern).
- Schema: `prisma/schema.prisma`. Output: `generated/prisma/` (non-default path — do not change).
- The datasource block has **no `url` field**. Connection string is passed at runtime via `PrismaPg` adapter using `env.DATABASE_URL`. CLI commands get it from `.env.local` via `dotenv -e .env.local --`.
- Soft-deletes only for `Complex` and `Field` (`isActive = false` + `deletedAt`). **Never hard-delete these entities.**
- Timestamps stored as UTC (`@db.Timestamptz`). Open/close hours stored as `HH:MM` strings interpreted in `Complex.timezone` (default: `America/Argentina/Buenos_Aires`).

### Key business rules

- A `FULL` field with `isDividable = true` auto-creates `HALF_A` + `HALF_B` sub-fields on creation.
- When a `Field` is created, `FieldWorkingSchedule` rows are **copied from the parent Complex's `ComplexWorkingSchedule`** — complex must have schedules configured first.
- Cannot soft-delete a `Field` with future `PENDING` or `CONFIRMED` bookings.
- `Booking.version` is used for optimistic locking.

---

## oRPC Error Codes

Defined on the `base` middleware: `RATE_LIMITED`, `UNAUTHORIZED`, `BAD_REQUEST`, `NOT_FOUND`, `CONFLICT`, `FORBIDDEN`, `INTERNAL_SERVER_ERROR`, `INPUT_VALIDATION_FAILED`, `OUTPUT_VALIDATION_FAILED`.

---

## Testing

- Framework: Vitest v3. No separate `vitest.config.ts` — Vitest picks up `vite.config.ts`.
- No test files exist yet; the harness (`@testing-library/react`, `jsdom`) is installed.
- No CI or pre-commit hooks configured.

---

## Agent System

- OpenCode agent prompts: `.opencode/agent/speckit-*.md`
- Claude Code orchestration rules: `CLAUDE.md`
- SpecKit workflow templates: `.specify/`
- Project skills: `.agents/skills/`
